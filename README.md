1. What is the difference between var, let, and const?
ans: var, let, and const are used to declare variables in JavaScript.

2. What is the spread operator (...)?
ans: The spread operator (...) is used to expand or copy elements from an array or object.


3. What is the difference between map(), filter(), and forEach()?
ans: These are array methods used to loop through arrays.

4. What is an arrow function?
ans: shorter way to write functions in JavaScript.

5. What are template literals?
   ans: allows to write strings with variables easily using backticks `.
